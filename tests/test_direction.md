# Lichen Test Direction

This workspace validates the standalone `lichen` blockwise hidden-memory construction.

All tests use `window_size = 2`. The notebook mirrors the earlier env tests, but the object under test is now the blockwise hidden-memory sampler instead of the segmentwise one.
