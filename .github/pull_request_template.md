## Summary

## Validation

- [ ] `python -m pytest -q`
- [ ] `python -m build`
- [ ] wheel and sdist install checks, if packaging changed

## Notes

- Keep the `lichen` import package name unchanged.
- Avoid monorepo-specific paths or assumptions.
